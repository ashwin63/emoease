"use client";

export default function ChatBubble() {
  return (
    <div 
      className="w-64 h-64 bg-gradient-to-b from-white to-blue-500 rounded-full"
      style={{
        boxShadow: '0 0 40px rgba(59, 130, 246, 0.5)'
      }}
    />
  );
}
