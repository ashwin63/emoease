"use client";

import { motion } from 'framer-motion';

export default function ChatBubble() {
  return (
    // Test with a simple colored div first
    <div className="w-40 h-40 bg-blue-500 rounded-full">
    </div>
  );
}
